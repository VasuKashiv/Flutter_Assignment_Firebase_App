import 'package:flutter/material.dart';

const kBorderDecoration = OutlineInputBorder(
  borderSide: BorderSide(color: Colors.grey, width: 2),
  borderRadius: BorderRadius.all(
    Radius.circular(10),
  ),
);
